import { useState } from 'react'
import type React from 'react'
import { api } from '../../api'
import type { CameraSource, CameraSourceType } from '../../types'
import { Field, Modal, Notice } from '../ui/primitives'

export function EditRoomCameraModal({ roomId, camera, onClose, onSaved }: { roomId: string; camera: CameraSource; onClose: () => void; onSaved: (camera: CameraSource) => void }) { const [label, setLabel] = useState(camera.label); const [sourceType, setSourceType] = useState(camera.source_type); const [source, setSource] = useState(camera.source); const [enabled, setEnabled] = useState(camera.is_enabled); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); try { onSaved(await api.updateRoomCamera(roomId, camera.id, { label: label.trim(), source_type: sourceType, source: source.trim(), is_enabled: enabled })) } catch (e) { setError(e instanceof Error ? e.message : 'Unable to update camera.') } finally { setBusy(false) } }; return <Modal title={`Edit ${camera.label}`} onClose={onClose}><form onSubmit={submit} className="space-y-4"><Field label="Camera label" value={label} onChange={(event) => setLabel(event.target.value)} placeholder="Front camera" required /><label className="block"><span className="mb-1.5 block text-xs font-bold uppercase tracking-wide text-[var(--muted)]">Source type</span><select value={sourceType} onChange={(event) => setSourceType(event.target.value as CameraSourceType)} className="w-full rounded-md border border-[var(--line)] bg-white px-3.5 py-3 text-sm"><option value="webcam">Webcam</option><option value="ip_stream">IP stream</option><option value="video_file">Video file</option></select></label><Field label="Source" value={source} onChange={(event) => setSource(event.target.value)} placeholder={sourceType === 'webcam' ? '0' : 'Stream URL or file path'} required /><label className="flex items-center gap-2 text-sm font-semibold"><input type="checkbox" checked={enabled} onChange={(event) => setEnabled(event.target.checked)} /> Enabled for recognition</label>{error && <Notice tone="error">{error}</Notice>}<button disabled={busy} className="w-full rounded-md bg-[var(--green)] py-3 text-sm font-bold text-white disabled:opacity-50">{busy ? 'Saving…' : 'Save camera'}</button></form></Modal> }
